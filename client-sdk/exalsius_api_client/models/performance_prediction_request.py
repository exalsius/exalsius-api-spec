# coding: utf-8

"""
exalsius API

The exalsius REST API enables programmatic access to GPU infrastructure management and orchestration capabilities. Access the API through the `exls` command-line tool or integrate it directly into your applications using standard HTTP requests. The API covers several areas: * **GPU Market Offers** Browse and compare GPU instance pricing across public cloud providers and hyperscalers. * **Operator Integration** Coordinates with the [exalsius-operator](https://github.com/exalsius/exalsius-operator) running in a management Kubernetes cluster to handle infrastructure provisioning and node lifecycle management. * **Node Management** Import cloud-provider instances or self-managed nodes (via SSH) into your node pool. Hardware characteristics of self-managed nodes are discovered automatically. * **Cluster Provisioning** Create and manage Kubernetes clusters on supported cloud providers or self-managed bare-metal infrastructure. * **Service Deployment** Deploy infrastructure services such as the NVIDIA GPU Operator, KubeRay, Flyte, or Kubeflow onto your clusters. * **Workspace Deployment** Provision application workloads including Jupyter Notebook servers, LLM inference services, and other compute workloads on your clusters.

The version of the OpenAPI document: 1.35.0
Contact: support@exalsius.ai
Generated by OpenAPI Generator (https://openapi-generator.tech)

Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations

import json
import pprint
import re  # noqa: F401
from typing import Any, ClassVar, Dict, List, Optional, Set

from pydantic import BaseModel, ConfigDict, Field, StrictStr, field_validator
from typing_extensions import Annotated, Self


class PerformancePredictionRequest(BaseModel):
    """
    PerformancePredictionRequest
    """  # noqa: E501

    model_name: StrictStr = Field(
        description="Define the model for which you want a performance prediction."
    )
    optimizer: StrictStr = Field(description="The optimizer selected for training.")
    batch_size: Annotated[int, Field(strict=True, ge=1)] = Field(
        description="The batch size."
    )
    sequence_length: Annotated[int, Field(strict=True, ge=1)] = Field(
        description="The sequence length."
    )
    accumulation_steps: Annotated[int, Field(strict=True, ge=1)] = Field(
        description="The number of gradient accumulation steps. Select 1 for no gradient accumulation."
    )
    dataset_size: Annotated[int, Field(strict=True, ge=1)] = Field(
        description="The size of the dataset in tokens."
    )
    __properties: ClassVar[List[str]] = [
        "model_name",
        "optimizer",
        "batch_size",
        "sequence_length",
        "accumulation_steps",
        "dataset_size",
    ]

    @field_validator("model_name")
    def model_name_validate_enum(cls, value):
        """Validates the enum"""
        if value not in set(
            [
                "LLAMA3_2_1B",
                "LLAMA3_2_3B",
                "LLAMA3_2_3B_INSTRUCT",
                "GEMMA3_1B_IT",
                "QWEN2_5_3B_INSTRUCT",
                "GPT2",
            ]
        ):
            raise ValueError(
                "must be one of enum values ('LLAMA3_2_1B', 'LLAMA3_2_3B', 'LLAMA3_2_3B_INSTRUCT', 'GEMMA3_1B_IT', 'QWEN2_5_3B_INSTRUCT', 'GPT2')"
            )
        return value

    @field_validator("optimizer")
    def optimizer_validate_enum(cls, value):
        """Validates the enum"""
        if value not in set(["adam", "adamw"]):
            raise ValueError("must be one of enum values ('adam', 'adamw')")
        return value

    model_config = ConfigDict(
        populate_by_name=True,
        validate_assignment=True,
        protected_namespaces=(),
    )

    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        # TODO: pydantic v2: use .model_dump_json(by_alias=True, exclude_unset=True) instead
        return json.dumps(self.to_dict())

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Self]:
        """Create an instance of PerformancePredictionRequest from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        """
        excluded_fields: Set[str] = set([])

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            exclude_none=True,
        )
        return _dict

    @classmethod
    def from_dict(cls, obj: Optional[Dict[str, Any]]) -> Optional[Self]:
        """Create an instance of PerformancePredictionRequest from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        _obj = cls.model_validate(
            {
                "model_name": obj.get("model_name"),
                "optimizer": obj.get("optimizer"),
                "batch_size": obj.get("batch_size"),
                "sequence_length": obj.get("sequence_length"),
                "accumulation_steps": obj.get("accumulation_steps"),
                "dataset_size": obj.get("dataset_size"),
            }
        )
        return _obj
