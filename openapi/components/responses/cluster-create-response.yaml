description: Cluster successfully created
content:
  application/json:
    schema:
      type: object
      required: [data]
      properties:
        data:
          type: object
          required: [cluster_id]
          properties:
            cluster_id:
              type: string
              format: int64
              description: The unique identifier for the cluster
              example: 123
