title: SshKeysListResponse
description: List of SSH keys
type: object
properties:
  ssh_keys:
    type: array
    items:
      properties:
        id:
          type: integer
          format: int64
          description: The ID of the SSH key
          example: 123
        name:
          type: string
          description: The name of the SSH key
          example: "my-ssh-key"
required:
  - ssh_keys
