title: SshKeyCreateResponse
description: SSH key creation response
type: object
properties:
  ssh_key_id:
    type: integer
    format: int64
    description: The ID of the created SSH key
    example: 123
required:
  - ssh_key_id
