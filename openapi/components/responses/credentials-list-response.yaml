description: List of available credentials
content:
  application/json:
    schema:
      $ref: '../schemas/credentials-list-response.yaml'
    examples:
      success:
        summary: A list of available credentials
        value:
          credentials:
            - name: "my-credentials"
              description: "These are credentials for the AWS cloud provider"
          total: 1