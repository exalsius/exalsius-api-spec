description: List of nodes in the cluster
content:
  application/json:
    schema:
      type: object
      properties:
        cluster_id:
          type: string
        nodes:
          type: array
          items:
            $ref: '../../components/schemas/node.yaml'
      required:
        - cluster_id
        - nodes

