description: List of nodes in the node pool
headers:
  X-Total-Count:
    description: Total number of available nodes
    schema:
      type: integer
      format: int32
content:
  application/json:
    schema:
      $ref: '../schemas/nodes-list-response.yaml'