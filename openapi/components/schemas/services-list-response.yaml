type: object
required:
  - services
properties:
  services:
    type: array
    items:
      $ref: './service-deployment.yaml'
  total:
    type: integer
    format: int32
    description: The total number of services
