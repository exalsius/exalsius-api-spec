title: NodesListResponse
type: object
required:
  - nodes
  - total
properties:
  nodes:
    type: array
    items:
      $ref: './node-response.yaml'
  total:
    type: integer
    format: int32
    description: Total number of nodes in the current result set