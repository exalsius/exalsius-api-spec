type: object
required:
  - credentials
properties:
  credentials:
    type: array
    items:
      $ref: './credentials.yaml'
  total:
    type: integer
    format: int32
    description: The total number of available credentials
