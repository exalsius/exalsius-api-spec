type: object
required:
  - service_templates
properties:
  service_templates:
    type: array
    items:
      $ref: './service-template.yaml'
  total:
    type: integer
    format: int32
    description: The total number of service templates
