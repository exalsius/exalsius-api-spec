type: object
required:
  - name
  - description
properties:
  name:
    type: string
    description: The name of the credentials
    example: "my-credentials"
  description:
    type: string
    description: The description of the credentials
    example: "These are credentials for the AWS cloud provider"